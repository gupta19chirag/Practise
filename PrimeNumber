#include<stdio.h>
#include<conio.h>
void main()
{
int num,r,i;
clrscr();
printf("Enter the number:");
scanf("%d",&num);
while(num>0){
r = num % 10;
for(i=2;i<r;i++){
if(num % i == 0)
break;
}
}
if(r == i)
printf("%d ",i);
num = num /10;
getch();
}
