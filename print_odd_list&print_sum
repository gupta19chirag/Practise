#include<stdio.h>
#include<conio.h>
void main()
{
int N,i,sum=0;
clrscr();
printf("Entre the number upto which you want the list");
scanf("%d",&N);
for(i=1;i<=N;i++){
if(i % 2 != 0){
printf("\n%d",i);
sum = sum + i;
}
}
printf("The sum is as follows: %d",sum);
getch();
}
